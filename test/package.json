{
  "name": "meteor-graphql-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "pretest": "DETECT_CHROMEDRIVER_VERSION=true meteor npm install chromedriver --no-save",
    "test": "METEOR_PACKAGE_DIRS=.. meteor test-packages --driver-package meteortesting:mocha ../"
  },
  "dependencies": {
    "@babel/runtime": "^7.9.2",
    "graphql": "^14.7.0",
    "graphql-tools": "^4.0.7",
    "meteor-node-stubs": "^1.0.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "selenium-webdriver": "^3.6.0",
    "sinon": "^9.0.1"
  }
}
